name: deploy

"on":
  push:
    branches: [main]
  workflow_run:
    workflows: ["test"]
    types:
      - completed

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - run: echo "🎉 This should send a mail for deployemnt."
      - name: Deploy to netlify
      - run: echo "🍏 This has been deployed to netflify."
